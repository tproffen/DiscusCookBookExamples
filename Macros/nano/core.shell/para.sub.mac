#
#  Create the radius for an individual ZnS sphere
#
radius_sub = logn(ab_shell,si_shell)
#radius_sub = max(8.0,radius_sub)
radius_sub = min(radius_sub,ab_core)
#
#fopen 3,radii,append
#fput 3, radius_sub
#fclose 3
